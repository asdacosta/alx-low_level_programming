#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @m: First operand
 * @n: Second operand
 *
 * Return: Result of the operands
 */
int add(int m, int n)
{
	int result;

	result = m + n;
	return (result);
}

